# Dependencias
node_modules/

# Variables de entorno (¡NUNCA subir las claves de Supabase a GitHub!)
.env
.env.local

# Archivos del sistema operativo
.DS_Store
Thumbs.db

# Backups de base de datos (contienen datos de clientes/ventas, nunca a git)
backups/